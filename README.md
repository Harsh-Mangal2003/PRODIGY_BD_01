

This project is a simple RESTful API built with **Node.js** and **Express.js** to perform basic CRUD (Create, Read, Update, Delete) operations on a user resource.

## 🔧 Features

- In-memory storage using JavaScript `Map`
- Full CRUD functionality
- Input validation (e.g., email format and age range)
- UUID for user identification
- Proper status codes and error handling

